<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Verification</title>
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap");

      body {
        font-family: "Inter", sans-serif;
        margin: 0;
        padding: 20px;
        box-sizing: border-box;
      }

      .certificate {
        max-width: 800px;
        margin: 0 auto;
        background-color: white;
        padding: 40px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        position: relative;
        overflow: hidden;
        border: 1px solid #ccc;
      }

      .watermark {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        opacity: 0.07;
        width: 80%;
        height: auto;
        z-index: 0;
      }

      .header {
        text-align: center;
        margin-bottom: 40px;
        position: relative;
        z-index: 1;
      }

      .logo {
        width: 80px;
        height: auto;
        margin-bottom: 5px;
      }

      .title {
        color: #005999;
        font-size: 24px;
        font-weight: bold;
        margin: 10px 0 30px;
      }

      .content {
        position: relative;
        z-index: 1;
        text-align: center;
        font-size: 16px;
        line-height: 1.6;
      }

      .otp-box {
        margin: 30px 0;
        padding: 20px;
        border-radius: 5px;
        border-left: 4px solid #005999;
        text-align: center;
      }

      .otp-code {
        font-size: 36px;
        letter-spacing: 8px;
        color: #005999;
        font-weight: bold;
        margin: 20px 0;
      }

      .warning {
        margin: 30px 0;
        padding: 20px;
        border-radius: 5px;
        border-left: 4px solid #00a86b;
        background-color: transparent;
        text-align: left;
      }

      .footer {
        margin-top: 40px;
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
        position: relative;
        z-index: 1;
      }

      .qr-code {
        width: 100px;
        height: 100px;
      }

      .address {
        font-size: 14px;
        color: #666;
        margin-top: 10px;
        max-width: 300px;
      }

      .stamp {
        text-align: center;
        padding: 20px;
        border: 2px solid #005999;
        border-radius: 50%;
        width: 120px;
        height: 120px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #005999;
      }

      .bottom-bar {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        height: 10px;
        background-color: #005999;
      }

      .bilingual {
        display: flex;
        justify-content: space-between;
        margin: 15px 0;
        align-items: center;
      }
    </style>
  </head>
  <body>
    <div class="certificate">
      <img src="<%= logo %>" alt="Watermark" class="watermark" />

      <div class="header">
        <img src="<%= logo %>" alt="GST Logo" class="logo" />
        <div style="color: #005999; font-size: 14px"><%= company %></div>
        <h1 class="title"><%= title %></h1>
      </div>

      <div class="content">
        <div class="bilingual">
          <p><%= greeting.en %></p>
          <p><%= greeting.ar %></p>
        </div>

        <div class="bilingual">
          <p><%= message.en %></p>
          <p style="text-align: right"><%= message.ar %></p>
        </div>

        <div class="otp-box">
          <div
            style="
              font-weight: 600;
              color: #005999;
              margin-bottom: 10px;
              font-size: 18px;
            "
          >
            <%= otpLabel.en %> / <%= otpLabel.ar %>
          </div>
          <div class="otp-code"><%= otp %></div>
        </div>

        <div class="warning">
          <div class="bilingual">
            <p><%= warning.en %></p>
            <p style="text-align: right"><%= warning.ar %></p>
          </div>
        </div>

        <div class="bilingual" style="color: #666; font-size: 14px">
          <p><%= disclaimer.en %></p>
          <p style="text-align: right"><%= disclaimer.ar %></p>
        </div>
      </div>

      <div class="footer">
        <div>
          <img src="<%= qrCode %>" alt="QR Code" class="qr-code" />
          <div class="address"><%= address %></div>
        </div>
        <div class="stamp">
          <div>
            <div><%= stamp.text %></div>
            <div style="font-size: 24px; font-weight: bold">
              <%= stamp.status %>
            </div>
            <div><%= stamp.year %></div>
          </div>
        </div>
      </div>

      <div class="bottom-bar"></div>
    </div>
  </body>
</html>

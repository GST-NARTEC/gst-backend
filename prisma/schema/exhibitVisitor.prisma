model ExhibitVisitor {
    id              String @id @default(cuid())
    
    name            String?
    email           String?
    phone           String?
    company         String?

    createdAt       DateTime? @default(now())
    updatedAt       DateTime? @updatedAt

    @@index([name, email, phone, company])
}
model Vat {
    id        String   @id @default(cuid())
    name      String?
    description String?
    value     Float?
    type      String?
    createdAt DateTime  @default(now())
    updatedAt DateTime  @updatedAt
}

